# Archivo histórico (no usar)

Copia antigua del modo Impostor **antes** de migrar a `cogs/impostor/`.

- El bot carga **`cogs.impostor`** (`main.py`), no esta carpeta.
- Las subcarpetas `impostor/` y `economia/` fueron **eliminadas** del repo (código activo solo en `cogs/`).
- Podés borrar también este `README` si ya no lo necesitás.
